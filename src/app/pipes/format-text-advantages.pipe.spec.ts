import { FormatTextAdvantagesPipe } from './format-text-advantages.pipe';

describe('FormatTextAdvantagesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTextAdvantagesPipe();
    expect(pipe).toBeTruthy();
  });
});
