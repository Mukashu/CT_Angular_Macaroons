import { AddEffectsToButtonsDirective } from './add-effects-to-buttons.directive';

describe('AddEffectsToButtonsDirective', () => {
  it('should create an instance', () => {
    const directive = new AddEffectsToButtonsDirective();
    expect(directive).toBeTruthy();
  });
});
